---
acceptance_id: AC-20260712-01
task_id: TASK-PRODUCTION-PERSISTENCE-LMSTUDIO-20260712
intent_id: TASK-PRODUCTION-PERSISTENCE-LMSTUDIO-20260712
owner: shipyard-cp
status: pending
reviewed_at: 2026-07-12
reviewed_by: Codex
approval_type: technical
release_approval_id: null
---

# Acceptance Record: Production Persistence and LM Studio / LM Link

## Implemented Evidence

- [x] Redis v2 namespace、長期TTL、productionのRedis必須設定、readiness接続を実装。
- [x] Task、Job、Result、event、audit、checkpoint、repo policy、publish idempotency、retrospectiveのRedis保存・復元を実装。
- [x] Task version CAS、state index更新、task index更新をLua transactionへ統合。
- [x] Redis clearは`KEYS`ではなく`SCAN`を使用。
- [x] 非同期`ControlPlaneRepository`、Redis / InMemory実装、executor instanceによるorphan回収を追加。
- [x] Redis障害を503、CAS競合を409へ正規化し、state APIをfail-closedにした。
- [x] LM Studio OpenAI互換adapter、LM Link透過metadata、ゼロ価格記録を実装。
- [x] LM Studio profileで`response_format: json_schema`を指定し、plan/devの機械可読JSONをgrammar制約する。
- [x] `codex` / `claude_code` のlow-risk plan/dev routing、plan限定fallback、backend/audit記録を実装。
- [x] `.env.example`、README、LM Studio / LM Link Quickstartを更新。

## Automated Evidence

- `pnpm run check:all`: pass（backend/frontend build、型、lint、test、module size、docs freshness）
- `pnpm run test:backend -- --reporter=dot`: 111 files / 2,303 tests pass、15 skippedを3回連続成功（LM Studio JSON schema回帰を含む）。
- `pnpm run test:load`: 5 tests pass（250 concurrent create/retrieve、mixed CRUD、health、memory stability）。
- `pnpm exec vitest run test/store-backend.test.ts test/routes.test.ts test/lmstudio-adapter.test.ts test/litellm-openai-compatible.test.ts --reporter=dot`: 4 files / 109 tests pass（LM Studio JSON schema回帰を含む）。
- `pnpm run check:modules` / `pnpm run check:docs`: pass。`control-plane-store.ts`の既存大型例外は技術的負債台帳で2026-09-30まで管理。

## Manual Execution Evidence

- 2026-07-12: 実LM Studio（app 0.4.2+2 / `lms` 1.3.3）で`127.0.0.1:1234` serverを起動し、`qwen/qwen3.6-27b`（17.48GB、context 4,096、parallel 1、TTL 30分）をloadした。
- `GET /v1/models`は`qwen/qwen3.6-27b`を返し、`POST /v1/chat/completions`の`response_format.type=json_schema`で有効なJSONを返した。これはLM Studio公式の構造化出力APIを使う実機確認である。
- 更新済みShipyard（`127.0.0.1:3101`）で、low-risk Task `task_f5d9e5ccfe404d889a1793f0d3f11bdf` のplan Job `job_79e8c9020c844297bffabcecc8e62aaf` とdev Job `job_bfcc2c66dcb34066b7a0f85534b860aa` が成功した。
- dev Result: `execution_backend=lmstudio`、`provider=lmstudio`、`model=qwen/qwen3.6-27b`、input 870 tokens、output 44 tokens、`cost_usd=0`、artifact 7件、`tool_plan_execution_verdict=executed`。監査イベント`audit_e4b62cbf85064b8b8885b0931d2bba65`（plan）と`audit_f74d642b7c2e470ca2827425201f783c`（dev）は、`worker_type=codex`、`execution_backend=lmstudio`、fallbackなしを記録した。
- 構造化出力制約を導入する前の実機試験は`structured_output_parse_error`で安全に拒否され、これを契機にJSON schema指定と回帰テストを追加した。raw output、prompt、Authorization tokenはこの記録へ保存していない。

## External Blocker State

- 2026-07-12: Docker DesktopのLinux Engineは`HCS_E_HYPERV_NOT_INSTALLED`で停止した。`wsl.exe --install --no-distribution`とWindows再起動後も、`systeminfo`は`Virtualization Enabled In Firmware: No`、`HypervisorPresent: false`を返した。BIOS/UEFIでCPU仮想化（Intel VT-x/VT-dまたはAMD SVM）を有効化しない限り、Docker composeを実行できない。
- 2026-07-12: LM Studioの実local API検証は完了した。LM Link remote deviceにのみ存在するモデルを使う実証は、接続済みremote deviceがないため未実施である。
- Docker compose再起動試験、LM Link remote試験、release tagの署名/SBOM/attestationが未完了のため、release tagはNo-Goのままとする。

## Outstanding Manual Evidence

- [ ] composeでAPI・Redisを再起動し、Task/Run/Event/Audit/Checkpoint/manual checklistの保持を確認する。
- [ ] Redis停止中の`/healthz=200`、`/health/ready=503`、state API=503と復旧保持を確認する。
- [x] 実LM Studioの`/v1`を使い、low-risk plan/devのJob/Result/artifactを確認する。
- [ ] LM Link remote deviceにだけあるモデルで、同じlocalhost API surfaceを通ることを確認する。
- [ ] release tagの署名、SBOM attestation、GHCR digest一致を確認する。
